# MongoDB Connection
# For local development, use your local MongoDB:
MONGODB_URI=mongodb://localhost:27017/auction-platform

# JWT Configuration (change this to a secure random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRE=7d

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# ============================================
# LOCAL SETUP INSTRUCTIONS:
# 1. Copy this file to .env: copy .env.example .env
# 2. Make sure MongoDB is running locally
# 3. Run: npm install
# 4. Run: npm run seed  (creates test users)
# 5. Run: npm run dev
#
# TEST ACCOUNTS (after running seed):
# Admin:   admin@auction.com / admin123
# Seller:  seller1@auction.com / seller123
# Bidder:  bidder1@auction.com / bidder123
# ============================================
